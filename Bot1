reg = input('Для начало зарегистрируйтесь [нажать "R"], если не впервый раз введите "логин":').lower()
def log_reg(reg):
  if reg != 'логин':
    if reg == 'r':
      log = input('Введите логин:')
      if len(log) >= 5:
        print('Вы успешно зарегестрировались!')
        return log
      else:
        log_reg(reg)
  elif reg == 'логин':
    log = input('Введите логин:')
    if log == log:
      print('Успешная авторизация!')
      return log
    else:
      print('Ошибка! Повторите ещё раз)
      log_reg(reg)
  else:
    print('Ошибка! Повторите ещё раз)
    log_reg(reg)
start = log_reg(reg)
print('Вы успешно вошли в акаунт' , start + '!')
print('Чтобы продолжить использовать бота, выберите следующее действие:')
print('1 - купить мерч')
print('2 - заказать рекламу'
print('help - написать в поддержку')
print('off - выключить бота')
begining = input('Действие(№):')
while begining != 'off':
  if begining == '1':
    print('Мерч огненный - 3000 руб')
    print('Мерч ледяной - 2900 руб')
    tipe = input('Выберите тип мерча:')
